// 1. You can't define a variable more than once
let petName = 'Hal'
// to replace petName
petName = 'Spot'

console.log('PetName: ' + petName)

// 2. There are rules related to variable name
    // --> variable name has to start with 
    // --> a letter A through Z capital or lowercase 
    // --> dollar sign( $ ) or underscore ( _ )

// 3. Variables names cannot be reserved keywords
    
